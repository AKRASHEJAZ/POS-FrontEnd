/// Default list page size (matches backend default).
const int kDefaultPageSize = 10;

/// Page-size choices shown in list screens (backend max is 100).
const List<int> kPageSizeOptions = [10, 25, 50, 100];

/// Max page size allowed by backend — use for filter dropdown option lists.
const int kPickerPageSize = 100;
